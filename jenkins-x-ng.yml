buildPack: none
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        agent:
          image: jenkinsxio/builder-go:0.1.255
        stages:
          - name: ci
            steps:
              - command: jx/scripts/ci-ng.sh
                name: runci
