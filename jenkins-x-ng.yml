buildPack: none
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        agent:
          image: jenkinsxio/builder-go:0.0.0-SNAPSHOT-PR-3443-1
        stages:
          - name: ci
            steps:
              - command: jx/scripts/ci-ng.sh
                name: runci
