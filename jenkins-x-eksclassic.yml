buildPack: none
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        agent:
          image: gcr.io/jenkinsxio/builder-go-maven:0.1.540
        stages:
          - name: ci
            steps:
              - command: jx/bdd/eksclassic/ci.sh
                name: runci
