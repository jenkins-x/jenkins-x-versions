buildPack: none
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        agent:
          image: 0.1.334
        stages:
          - name: ci
            steps:
              - command: jx/scripts/ci-gitops.sh
                name: runci
