buildPack: none
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        agent:
          image: jenkinsxio/builder-go:0.0.0-SNAPSHOT-PR-3458-5
        stages:
          - name: ci
            steps:
              - command: jx/scripts/ci-gitops.sh
                name: runci
