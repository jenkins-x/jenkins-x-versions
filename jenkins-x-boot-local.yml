buildPack: none
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        agent:
          image: jenkinsxio/builder-go:0.0.0-SNAPSHOT-PR-4537-9
        stages:
          - name: ci
            steps:
              - command: jx/bdd/boot-local/ci.sh
                name: runci
