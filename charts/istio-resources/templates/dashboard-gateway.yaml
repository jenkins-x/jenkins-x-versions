apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: dashboard-gw
  namespace: jx
spec:
  selector:
    app: dex-gateway
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - {{ .Values.dashboard.host }}