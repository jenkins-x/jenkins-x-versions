buildPack: none
pipelineConfig:
  pipelines:
    release:
      pipeline:
        agent:
          image: gcr.io/jenkinsxio/builder-go-maven:0.1.537
        stages:
          - name: ci
            steps:
              - command: jx/bdd/tekton/ci.sh
                name: runci
