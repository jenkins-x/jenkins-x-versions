buildPack: none
pipelineConfig:
  pipelines:
    release:
      build:
        steps:
        - name: release-version
          command: jx
          args:
          - step
          - changelog
          - --verbose
          - --version
          - ${VERSION}
          - --rev
          - ${PULL_BASE_SHA}
