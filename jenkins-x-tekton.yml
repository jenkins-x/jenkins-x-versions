buildPack: none
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        agent:
          image: jenkinsxio/builder-go:SNAPSHOT-JX-PR-3252-1
        stages:
          - name: ci
            steps:
              - command: jx/scripts/ci-tekton.sh
                name: runci
