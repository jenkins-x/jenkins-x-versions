buildPack: none
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        agent:
          image: jenkinsxio/builder-go-maven:0.0.0-SNAPSHOT-PR-4466-4
        stages:
          - name: ci
            steps:
              - command: jx/bdd/tekton/ci.sh
                name: runci
