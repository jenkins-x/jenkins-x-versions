buildPack: none
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        agent:
          image: jenkinsxio/builder-go:0.0.0-SNAPSHOT-PR-3648-47
        stages:
          - name: ci
            steps:
              - command: jx/scripts/ci-tekton.sh
                name: runci
