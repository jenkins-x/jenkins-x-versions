buildPack: none
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        agent:
          image: jenkinsxio/builder-go-maven:0.0.0-SNAPSHOT-PR-3504-1
        stages:
          - name: ci
            steps:
              - command: jx/scripts/ci-prow.sh
                name: runci
