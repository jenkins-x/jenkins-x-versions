buildPack: none
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        agent:
          image: jenkinsxio/builder-go:0.0.0-SNAPSHOT-PR-3504-4
        stages:
          - name: ci
            steps:
              - command: jx/scripts/ci-prow.sh
                name: runci
