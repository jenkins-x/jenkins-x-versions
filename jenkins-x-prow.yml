buildPack: none
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        agent:
          image: jenkinsxio/builder-go:0.0.0-SNAPSHOT-PR-3805-7
        stages:
          - name: ci
            steps:
              - command: jx/bdd/knative-build/ci.sh
                name: runci
