buildPack: none
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        agent:
          image: jenkinsxio/builder-go-maven:0.1.273
        stages:
          - name: ci
            steps:
              - command: jx/scripts/ci-prow.sh
                name: runci
