buildPack: none
pipelineConfig:
  agent:
    container: gcr.io/jenkinsxio/builder-go
  pipelines:
    pullRequest:
      pipeline:
        stages:
          - name: ci
            steps:
              - command: jx/bdd/knative-build/ci.sh
                name: runci
