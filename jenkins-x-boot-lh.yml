buildPack: none
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        agent:
          image: gcr.io/jenkinsxio/builder-go-maven:0.1.594
        stages:
          - name: ci
            steps:
              - command: jx/bdd/boot-lh/ci.sh
                name: runci
