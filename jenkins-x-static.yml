buildPack: none
pipelineConfig:
  agent:
    container: gcr.io/jenkinsxio/builder-go
  pipelines:
    pullRequest:
      pipeline:
        stages:
          - name: ci
            steps:
              - command: jx/bdd/static/ci.sh
                name: runci
