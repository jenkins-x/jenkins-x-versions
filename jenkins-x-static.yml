buildPack: none
pipelineConfig:
  pipelines:
    release:
      pipeline:
        agent:
          image: some-image
        stages:
          - name: BDD Tests on Static Jenkins
            steps:
              - command: jx/scripts/ci.sh
