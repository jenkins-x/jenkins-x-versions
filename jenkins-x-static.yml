buildPack: none
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        agent:
          image: gcr.io/jenkinsxio/builder-go-maven:0.1.424
        stages:
          - name: ci
            steps:
              - command: jx/bdd/static/ci.sh
                name: runci
